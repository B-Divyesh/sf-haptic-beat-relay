[
  {
    "id": "demo-sandbox",
    "claim": "Try a paired sample round without setup or saved data.",
    "where": "landing primary action, demo banner, README",
    "test": "npm run test:browser -- --grep @claim:demo-sandbox",
    "sandbox": "fresh browser context at /?demo=1; start and reset the seeded round while asserting no API requests or browser storage"
  },
  {
    "id": "sample-duration",
    "claim": "The sample round lasts 12 seconds.",
    "where": "demo setup, README, demo guide",
    "test": "npm run test:browser -- --grep @claim:sample-duration",
    "sandbox": "fresh browser context at /?demo=1; start the sample, verify it remains active before completion, then observe its 12-second completion"
  },
  {
    "id": "local-audio",
    "claim": "The audio loop stays on the host device.",
    "where": "landing facts, host file picker, privacy, README",
    "test": "npm run test:browser -- --grep @claim:local-audio",
    "sandbox": "fresh browser context; load a marked audio fixture and assert its bytes are never sent"
  },
  {
    "id": "no-third-party",
    "claim": "There are no advertising trackers or third-party runtime requests.",
    "where": "privacy, README",
    "test": "npm run test:browser -- --grep @claim:no-third-party",
    "sandbox": "fresh browser context; capture every request while loading a local audio fixture and assert every origin is the product origin"
  },
  {
    "id": "no-account",
    "claim": "No account is needed.",
    "where": "landing, privacy, README",
    "test": "npm run test:browser -- --grep @claim:no-account",
    "sandbox": "fresh browser context; create a working room without a sign-in step"
  },
  {
    "id": "free-use",
    "claim": "Haptic Beat Relay is free to use.",
    "where": "landing facts, terms, README",
    "test": "npm run test:browser -- --grep @claim:free-use",
    "sandbox": "fresh browser context; create a room and assert no purchase or payment gate appears"
  },
  {
    "id": "shared-score",
    "claim": "A friend receives each cue, taps back, and builds a shared accuracy score.",
    "where": "landing preview and steps, live room, README",
    "test": "npm run test:browser -- --grep @claim:shared-score",
    "sandbox": "two fresh browser contexts; create and join one room, start a round, receive a cue, return a tap, and see the same scored result"
  },
  {
    "id": "live-relay",
    "claim": "The deployed relay keeps fresh host and friend room, cue, tap, and score traffic connected.",
    "where": "README live verification and deployment sections",
    "test": "RELAY_ROUNDS=30 npm run test:live-relay",
    "sandbox": "30 fresh API rooms plus 30 fresh desktop-host and 390 px friend browser pairs against the live deployment; assert create, join, WebSocket cue, returned tap, shared score, and no browser errors"
  },
  {
    "id": "ephemeral-rooms",
    "claim": "Temporary room records survive a server restart and expire after two hours.",
    "where": "landing limits, privacy, README",
    "test": "cargo test claim_ephemeral_rooms_persist_across_restart_until_the_configured_ttl",
    "sandbox": "temporary SQLite file and short controllable TTL; assert a new server instance can join before expiry and rejects the room after expiry"
  },
  {
    "id": "rate-limit",
    "claim": "Each client may make exactly 40 room API requests per second; later requests return 429 with Retry-After.",
    "where": "privacy, README",
    "test": "RELAY_RATE_REPETITIONS=5 npm run test:live-rate-limit",
    "sandbox": "five distinct fresh forwarded client identities; each sends a 45-request live burst and observes exactly 40 successes, then five 429 responses with Retry-After: 1"
  },
  {
    "id": "health",
    "claim": "The health endpoint reports the build SHA.",
    "where": "README",
    "test": "npm run test:browser -- --grep @claim:health",
    "sandbox": "fresh request context; request /health and assert status plus a build_sha value"
  },
  {
    "id": "connection-required",
    "claim": "The real relay needs a connection.",
    "where": "landing facts",
    "test": "npm run test:browser -- --grep @claim:connection-required",
    "sandbox": "fresh browser context; go offline before creating a real room and assert the recovery message appears"
  },
  {
    "id": "visual-cue",
    "claim": "The screen flashes each cue when vibration is unavailable.",
    "where": "landing limits, README",
    "test": "npm run test:browser -- --grep @claim:visual-cue",
    "sandbox": "two fresh browser contexts with vibration unavailable; relay one cue and assert the friend tap pad enters its visual cue state"
  },
  {
    "id": "haptic-output",
    "claim": "Each friend cue uses phone vibration and connected controller haptics when the browser supports them.",
    "where": "landing, live friend view, terms, README",
    "test": "npm run test:browser -- --grep @claim:haptic-output",
    "sandbox": "two fresh browser contexts; stub both haptic APIs, relay one real cue, and assert vibrate(45) plus dual-rumble actuation"
  },
  {
    "id": "real-round-duration",
    "claim": "A real round lasts 60 seconds.",
    "where": "host round action, README",
    "test": "npm run test:browser -- --grep @claim:real-round-duration",
    "sandbox": "two fresh browser contexts; start a connected real round, assert it is active at 59 seconds, then measure completion at 60 seconds"
  },
  {
    "id": "singleton-deployment",
    "claim": "The live relay uses durable SQLite under /data and exactly one Container App replica for WebSocket delivery.",
    "where": "README deployment and privacy sections",
    "test": "npm run test:live-topology",
    "sandbox": "read-only Azure queries; assert the /data volume, one active revision, min/max one, HTTP ingress, one ready replica, full-SHA image, and matching live health identity"
  }
]
